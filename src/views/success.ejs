<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Application Submitted — Core Crew Logistics</title>
    <link rel="icon" type="image/jpeg" href="/public/img/logo.jpg" />
    <link rel="stylesheet" href="/public/styles.css" />
    <style>
      .success-wrapper {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 60vh;
        padding: var(--space-xl) var(--space-md);
      }
      .success-card {
        text-align: center;
        padding: var(--space-2xl) var(--space-xl);
        max-width: 480px;
        width: 100%;
        animation: successFadeUp 0.7s cubic-bezier(0.22, 1, 0.36, 1) both;
      }
      @keyframes successFadeUp {
        from { opacity: 0; transform: translateY(24px); }
        to   { opacity: 1; transform: translateY(0); }
      }
      .success-checkmark {
        width: 110px;
        height: 110px;
        margin: 0 auto var(--space-xl);
      }
      .success-checkmark .circle-bg {
        fill: rgba(46, 204, 113, 0.1);
      }
      .success-checkmark .circle {
        fill: none;
        stroke: var(--accent-green);
        stroke-width: 2.5;
        stroke-dasharray: 227;
        stroke-dashoffset: 227;
        animation: circleStroke 0.8s cubic-bezier(0.65, 0, 0.35, 1) 0.2s forwards;
      }
      @keyframes circleStroke {
        to { stroke-dashoffset: 0; }
      }
      .success-checkmark .check {
        fill: none;
        stroke: var(--accent-green);
        stroke-width: 3.5;
        stroke-linecap: round;
        stroke-linejoin: round;
        stroke-dasharray: 60;
        stroke-dashoffset: 60;
        animation: checkStroke 0.5s cubic-bezier(0.65, 0, 0.35, 1) 0.75s forwards;
      }
      @keyframes checkStroke {
        to { stroke-dashoffset: 0; }
      }
      .success-card h2 {
        font-size: 1.75rem;
        font-weight: 700;
        letter-spacing: -0.02em;
        margin-bottom: var(--space-sm);
        color: var(--text);
      }
      .success-card h2::after {
        content: '';
        display: block;
        width: 48px;
        height: 3px;
        border-radius: 3px;
        background: var(--accent-green);
        margin: var(--space-sm) auto 0;
      }
      .success-card p {
        font-size: 1.1rem;
        line-height: 1.7;
        max-width: 400px;
        margin: var(--space-md) auto;
        color: var(--text-muted);
      }
      .success-card .btn {
        margin-top: var(--space-lg);
        display: inline-flex;
        align-items: center;
        gap: 0.5em;
      }
    </style>
  </head>
  <body>
    <%- include('partials/header') %>
    <main class="container">
      <div class="success-wrapper">
        <div class="page-card success-card">
          <div class="success-checkmark">
            <svg viewBox="0 0 80 80">
              <circle class="circle-bg" cx="40" cy="40" r="36" />
              <circle class="circle" cx="40" cy="40" r="36" />
              <path class="check" d="M24 42 l10 10 l22 -24" />
            </svg>
          </div>
          <h2>Thank You!</h2>
          <p>Your application has been submitted successfully. We will contact you soon.</p>
          <a class="btn primary" href="/">&#8592; Back to Home</a>
        </div>
      </div>
    </main>
    <%- include('partials/footer') %>
  </body>
</html>