<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Verify Identity — CoreCrew Logistics</title>
    <link rel="stylesheet" href="/public/styles.css" />
  </head>
  <body>
    <%- include('partials/header') %>
    <main class="container">
      <div class="card">
        <h2>Verify your identity</h2>
        <p>To complete your application, verify your identity with ID.me. This helps keep applicants and clients safe.</p>
        <% if (idmeConfigured) { %>
          <div class="actions">
            <a class="btn primary" href="/auth/idme">Continue with ID.me</a>
            <a class="btn" href="/apply/submit">Skip for now</a>
          </div>
        <% } else { %>
          <p style="color:var(--muted)">ID.me is not configured yet. You can still proceed.</p>
          <div class="actions">
            <a class="btn primary" href="/apply/submit">Proceed to review</a>
          </div>
        <% } %>
      </div>
    </main>
    <%- include('partials/footer') %>
  </body>
  </html>

